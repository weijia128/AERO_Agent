# DSL 优化代办计划

## 背景
当前规则配置存在语义不统一与校验缺失的问题，计划逐步引入更严谨的 DSL 规范与兼容解析层。

## 计划列表
- [ ] 收敛 DSL 规范（when/all/any/not/atom 运算），定义字段路径与类型约束
- [ ] 实现解析器兼容层（旧 condition/map 写法自动映射到新 AST）
- [ ] 加入 schema 校验与最小单测覆盖关键规则场景

## 备注
- 优先保持对现有配置的兼容性
- 在未完成迁移前，避免破坏现有规则生效
